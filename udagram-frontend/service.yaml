apiVersion: v1
kind: Service
metadata:
  name: udagram-frontend
  labels:
    app: udagram-frontend
spec:
  selector:
    app: udagram-frontend
  ports:
    - name: "8100"
      port: 8100
      targetPort: 8100
  type: LoadBalancer